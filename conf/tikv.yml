---
global:
  log-level: "info"
  log-rotation-timespan: "24h"

readpool:
  storage:
  coprocessor:

server:
  snap-max-write-bytes-per-sec: "10MB"
  snap-max-total-size: "512MB"
  concurrent-send-snap-limit: 1024
  concurrent-recv-snap-limit: 1024

storage:

pd:
  endpoints: ["172.16.10.148:2379"]

metric:

raftstore:
  snap-gc-timeout: "30m"
  snap-mgr-gc-tick-interval: "1m"
  sync-log: false
  consistency-check-interval: "10m"

coprocessor:

rocksdb:
  defaultcf:
  writecf:
  lockcf:
raftdb:
  defaultcf:

security:

import:
