---
global:
  log-level: "info"
  log-rotation-timespan: "24h"

readpool:
  storage:
  coprocessor:

server:
  snap-max-write-bytes-per-sec: "50MB"
  snap-max-total-size: "10G"
  concurrent-send-snap-limit: 1024
  concurrent-recv-snap-limit: 1024

storage:

pd:
  endpoints: ["172.16.30.186:2379"]

metric:

raftstore:
  snap-gc-timeout: "30m"
  snap-mgr-gc-tick-interval: "1m"
  sync-log: false
  consistency-check-interval: "30m"

coprocessor:

rocksdb:
  defaultcf:
  writecf:
  lockcf:
raftdb:
  defaultcf:

security:

import:
