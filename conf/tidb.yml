---

global:
  token-limit: 10000

log:
  file:

security:

status:

performance:
  run-auto-analyze: false
  feedback-probability: 0.00

proxy_protocol:

prepared_plan_cache:
  enabled: true
  capacity: 1000

opentracing:
  sampler:
  reporter:

tikv_client:
  max-batch-size: 128
  overload-threshold: 100
  max-batch-wait-time: 20000000
  batch-wait-size: 16

txn_local_latches:
  enabled: false

binlog:
pessimistic-txn:
