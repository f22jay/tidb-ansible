---

global:
  token-limit: 10000
log:
  slow-threshold: 6000
  file:

security:
status:
performance:
  run-auto-analyze: false

proxy_protocol:

prepared_plan_cache:
  enabled: true
  capacity: 1000

opentracing:
  sampler:
  reporter:

tikv_client:
  grpc-connection-count: 4

  commit-timeout: "60s"

  max-batch-size: 128
  tikv-heavy-load-to-batch: 30
  batch-wait-time: 3000000
  batch-wait-size: 16

txn_local_latches:
  enabled: false

binlog:
