---
global:

security:

log:
  level: "info"

  file:
    log-rotate: true

metric:

schedule:
  max-snapshot-count: 1024
  max-pending-peer-count: 1024
  region-schedule-limit: 1024

replication:
  max-replicas: 3
